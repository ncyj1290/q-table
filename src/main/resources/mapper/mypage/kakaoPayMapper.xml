<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.itwillbs.qtable.mapper.mypage.kakoPayMapper">
	
	<insert id="insertPayment"  parameterType="com.itwillbs.qtable.vo.myPage.payment">
	insert into payment(
		member_idx,
		payment_date,
		payment_amount,
		pay_status,
		pay_way,
		pay_type,
		pay_reference,
		reference_idx
	) value (
		#{member_idx},
		NOW(),
		#{payment_amount},
		#{pay_status},
		#{pay_way},
		#{pay_type},
		#{pay_reference},
		#{reference_idx}
	)

	</insert>	
	
	
	
</mapper>